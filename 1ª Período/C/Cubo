#include <stdio.h>
// Alternativamente, usando #include <math.h>, pode-se usar o comando pronto pow(num,3);


int main()
{
    
   int num;

    printf("Digite um número: ");
    scanf("%d", &num);

    num = num * num * num;

    printf("O cubo do número é: %d\n", num);
    
    return 0;
}
